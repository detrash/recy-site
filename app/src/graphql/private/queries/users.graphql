query Users {
  users {
    id
    email
    profileType
    lastLoginDate
    phoneNumber
    forms {
      id

      documents {
        id
        residueType
        amount
        videoFileName
        invoicesFileName
      }

      isFormAuthorizedByAdmin
      formMetadataUrl
      walletAddress
      createdAt
    }
  }
}

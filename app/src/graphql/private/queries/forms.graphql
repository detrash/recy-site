query Forms {
  forms {
    id

    documents {
      id
      residueType
      amount
      videoFileName
      invoicesFileName
    }

    user {
      phoneNumber
      email
    }

    isFormAuthorizedByAdmin
    formMetadataUrl
    walletAddress
    createdAt
  }
}

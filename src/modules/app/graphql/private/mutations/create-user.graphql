mutation CreateUser(
  $profileType: ProfileType!
  $phoneNumber: String!
  $email: String!
) {
  createUser(
    data: {
      profileType: $profileType
      phoneNumber: $phoneNumber
      email: $email
    }
  ) {
    authUserId
    email
    profileType
    phoneNumber
  }
}
